// JavaScript Document
var damage = function(atkpower) {
	var randNum = Math.floor(Math.random()*20);
	console.log("the randnum is " + randNum);
	if (randNum < 1) {
hero.str * 2 === atkpower
	}
	else (randNum > 1) {
		hero.str === atkpower
	}
};